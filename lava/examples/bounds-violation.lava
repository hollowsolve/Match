~ A direct bound violation is rejected; the offending write is undone.
create container "Credit" of type int >= 0 with value 10
print(Credit)
update Credit to -5     ~ violates `>= 0` — fails here, Credit stays 10
print(Credit)          ~ never reached
